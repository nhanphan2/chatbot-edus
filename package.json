{
  "name": "firestore-chatbot-api",
  "version": "1.0.0",
  "description": "Firestore-based chatbot API deployed on Vercel",
  "main": "api/chatbot.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "vercel": "^32.7.2"
  },
  "engines": {
    "node": "18.x"
  }
}